# 📊 Аналитика: Контент Завод

> Обзор архитектуры, выполненные улучшения и план дальнейшего развития.

---

## Текущая архитектура

### Компоненты

```
[Form / Schedule / Manual Trigger]
        │
        ▼
  [⚙️ Конфигурация AI]  ←── GPTunnel / Ollama / OpenRouter / LM Studio
        │
        ▼
  [HTTP Request]  ←── retry: 3 раза, ожидание 5 сек
        │
        ▼
  [🔄 Обработка JSON]  ←── parseAIJson: parse → strip markdown → regex
        │
        ├──► [💾 PostgreSQL]  ←── content_results / workflow_errors
        │
        ├──► [👤 Модерация]  ←── Telegram approve/reject (Wait webhook)
        │
        └──► [📤 Публикация]  ←── Telegram / VK
```

### Workflow (12 файлов)

| #   | Название            | Триггер           | AI   | Retry | DB  | Форма |
| --- | ------------------- | ----------------- | ---- | ----- | --- | ----- |
| 00  | AI Config           | Sub-workflow      | —    | —     | —   | —     |
| 01  | Starter             | Manual + Schedule | ✅   | ✅    | ✅  | —     |
| 02  | Full Cycle          | Manual + Schedule | ✅×2 | ✅    | ✅  | —     |
| 03  | Competitor Analysis | Manual + Form     | ✅   | ✅    | ✅  | ✅    |
| 04  | Product Description | Manual + Form     | ✅   | ✅    | ✅  | ✅    |
| 05  | Review Responses    | Manual + Form     | ✅   | ✅    | ✅  | ✅    |
| 06  | Social Content      | Manual + Form     | ✅   | ✅    | ✅  | ✅    |
| 07  | Error Handler       | Error Trigger     | —    | —     | ✅  | —     |
| 08  | Telegram Publisher  | Sub-workflow      | —    | —     | —   | —     |
| 09  | VK Publisher        | Sub-workflow      | —    | —     | —   | —     |
| 10  | Content Moderator   | Sub-workflow      | —    | —     | —   | —     |
| 11  | Video Pipeline      | Manual            | ✅   | ✅    | ✅  | —     |

---

## ✅ Выполнено

### Фаза 1 — Стабилизация

- [x] **Централизованная конфигурация AI** — sub-workflow `00-ai-config.json` + N8N Variables ($vars)
- [x] **Retry на все HTTP Request** — `retryOnFail: true`, `maxTries: 3`, `waitBetweenTries: 5000`
- [x] **Улучшенный парсинг JSON** — `parseAIJson()`: `JSON.parse` → strip markdown → regex extract
- [x] **response_format: json_object** — передаётся в теле каждого AI-запроса
- [x] **Обработчик ошибок** — workflow 07: Error Trigger → Telegram + PostgreSQL
- [x] **Docker networking fix** — `host.docker.internal` вместо `localhost` для Ollama/LM Studio

### Фаза 2 — Хранение и планирование

- [x] **PostgreSQL хранилище** — автоматическое создание таблиц через `init-db.sql`
- [x] **Таблицы**: `content_results`, `workflow_errors`, `prompt_variants`
- [x] **Сохранение в БД** — PostgreSQL Insert вpоследнем шаге каждого workflow
- [x] **Schedule Trigger** — для 01, 02 (отключён по умолчанию, каждые 8 часов)
- [x] **Form Trigger** — для 03-06 (веб-формы для маркетплейс-параметров)
- [x] **MinIO безопасность** — пароль через env vars вместо хардкода

### Фаза 3 — Видео-пайплайн

- [x] **Сценарий** — генерация через AI (хронометраж, структура)
- [x] **TTS подготовка** — Edge TTS (бесплатно) / ElevenLabs (качество)
- [x] **FFmpeg команды** — шаблон для сборки видео

### Фаза 4 — Публикация и модерация

- [x] **Telegram Publisher** — текст + фото через Bot API (workflow 08)
- [x] **VK Publisher** — wall.post через VK API v5.199 (workflow 09)
- [x] **Модерация** — approve/reject/edit через Telegram + Wait webhook (workflow 10)
- [x] **N8N Variables** — `TELEGRAM_BOT_TOKEN`, `VK_ACCESS_TOKEN` через `$vars`

### Инфраструктура

- [x] **Обновлена документация** — README.md, workflows/README.md, .env.example
- [x] **Исправлен test-aggregators.js** — URL `gptunnel.ru/v1`, auth без Bearer
- [x] **Исправлен package.json** — удалены ссылки на несуществующие скрипты
- [x] **docker-compose.yml** — монтирование init-db.sql, MinIO env vars

---

## 🔄 План развития (не реализовано)

### Фаза 5 — A/B тестирование промтов

Таблица `prompt_variants` уже создана. Нужно:

- [ ] Workflow для A/B тестирования: 2 промта → 2 генерации → AI-оценка → запись метрик
- [ ] Дашборд: сравнение вариантов по `avg_score`, `usage_count`
- [ ] Автоматический выбор лучшего промта на основе статистики

### Фаза 6 — RAG и База знаний

- [ ] Сбор данных: парсинг сайтов → документы → embedding
- [ ] Хранение embedding в PostgreSQL (pgvector) или Qdrant
- [ ] Контекстные промты: "напиши как у [конкурент]" с примерами из базы

### Фаза 7 — Полноценное видео

- [ ] Интеграция с Replicate / RunwayML для генерации видеоряда
- [ ] Автоматический монтаж: TTS-аудио + изображения + субтитры
- [ ] Публикация на YouTube через API

### Фаза 8 — Аналитика публикаций

- [ ] Сбор статистики: просмотры, лайки, репосты (Telegram, VK)
- [ ] Связка: контент → публикация → метрики → обратная связь в промт
- [ ] Grafana дашборд с BI-метриками

### Фаза 9 — Расширение каналов

- [ ] Дзен (Yandex) Publisher
- [ ] Одноклассники Publisher
- [ ] Email-рассылки через SMTP
- [ ] RSS-фид для контента

---

## Метрики для отслеживания

### Качество генерации

```sql
-- Средний score по workflow
SELECT workflow_name, AVG(quality_score) as avg_score, COUNT(*) as total
FROM content_results
WHERE created_at > NOW() - INTERVAL '7 days'
GROUP BY workflow_name;

-- Процент одобрений модератором
SELECT
  COUNT(*) FILTER (WHERE status = 'approved') * 100.0 / COUNT(*) as approve_rate
FROM content_results
WHERE moderator_decision IS NOT NULL;
```

### Стабильность

```sql
-- Ошибки по workflow за неделю
SELECT workflow_name, error_type, COUNT(*) as errors
FROM workflow_errors
WHERE created_at > NOW() - INTERVAL '7 days'
GROUP BY workflow_name, error_type
ORDER BY errors DESC;

-- Среднее время ответа AI
SELECT workflow_name, AVG(generation_time_ms) as avg_ms
FROM content_results
GROUP BY workflow_name;
```

### Стоимость

```sql
-- Расход токенов по дням
SELECT DATE(created_at) as day,
       SUM(tokens_input + tokens_output) as total_tokens,
       SUM(tokens_input + tokens_output) * 0.0025 / 1000 as estimated_cost_usd
FROM content_results
GROUP BY DATE(created_at)
ORDER BY day DESC;
```

---

## Известные ограничения

1. **PostgreSQL credential** — необходимо создать вручную в N8N UI
2. **N8N Variables** — необходимо создать вручную (Settings → Variables)
3. **Video Pipeline** — генерирует команды, но не запускает FFmpeg/TTS автоматически
4. **Модерация** — требует настроенного Telegram бота
5. **VK API** — токен получается через Implicit Flow (срок действия ограничен)
6. **Schedule Triggers** — отключены по умолчанию (включите вручную)
7. **Ollama** — требует GPU с 8GB+ VRAM для больших моделей

---

_Последнее обновление: июнь 2025_
