events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 100M;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript
               application/json application/javascript application/xml+rss;

    # WebSocket upgrade mapping (для n8n push/SSE)
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    # Docker DNS resolver (127.0.0.11) — перерезолвивает IP при рестарте контейнеров
    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;

    # Upstreams
    upstream n8n_backend {
        server n8n:5678;
    }

    upstream dashboard_backend {
        server dashboard:3001;
    }

    # ─── HTTP → HTTPS (production) ───
    server {
        listen 80;
        server_name k-m-m.ru www.k-m-m.ru;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # ─── HTTPS (production) ───
    server {
        listen 443 ssl default_server;
        http2 on;
        server_name k-m-m.ru www.k-m-m.ru;

        ssl_certificate /etc/nginx/ssl/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_session_timeout 10m;
        ssl_session_cache shared:SSL:10m;

        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # Главная страница → Dashboard
        location / {
            set $dashboard http://dashboard:3001;
            proxy_pass $dashboard;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Dashboard API
        location /api/ {
            set $dashboard http://dashboard:3001;
            proxy_pass $dashboard;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Dashboard Socket.IO (WebSocket)
        location /ws/ {
            set $dashboard http://dashboard:3001;
            proxy_pass $dashboard;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # N8N статика — HTML ссылается на /assets/, /static/ от корня
        # Без этих блоков запросы уйдут в dashboard и фронт N8N не загрузится
        location /assets/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /static/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        # N8N REST API — фронтенд обращается к /rest/* от корня
        # Push WebSocket идёт на /rest/push — нужен отдельный блок ДО /rest/
        location /rest/push {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 600;
        }
        location /rest/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_cache off;
            proxy_connect_timeout 120;
            proxy_send_timeout 120;
            proxy_read_timeout 120;
        }
        # N8N Push (SSE/WebSocket) — для live-обновлений в редакторе
        location /push {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 600;
        }
        # N8N node types JSON — фронтенд грузит /types/nodes.json
        location /types/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        # N8N healthz — фронтенд делает heartbeat, отдаём 200 напрямую
        location /healthz {
            access_log off;
            return 200 '{"status":"ok"}';
            add_header Content-Type application/json;
        }
        location = /favicon.ico {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }

        # N8N Panel — trailing / в proxy_pass срезает /n8n/ префикс
        # N8N слушает на /, а N8N_EDITOR_BASE_URL говорит фронтенду добавлять /n8n/
        # Все запросы (/n8n/assets/*, /n8n/rest/*, /n8n/push и т.д.) идут сюда
        location /n8n/ {
            set $n8n http://n8n:5678;
            rewrite ^/n8n/(.*) /$1 break;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_buffering off;
            proxy_cache off;

            proxy_connect_timeout 600;
            proxy_send_timeout 600;
            proxy_read_timeout 600;
            send_timeout 600;
        }
        location = /n8n {
            return 301 /n8n/;
        }

        # N8N Webhooks (для Telegram бота и пайплайна — на корневом уровне)
        location /webhook/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass 1;
            proxy_no_cache 1;
        }

        # N8N Webhook Waiting (для Wait node resume)
        location /webhook-waiting/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass 1;
            proxy_no_cache 1;
        }

        # N8N Form Trigger
        location /form/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Статический контент
        location /output/ {
            alias /usr/share/nginx/html/output/;
            autoindex off;
            expires 7d;
            add_header Cache-Control "public, immutable";
        }

        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }

    # ─── Локальная конфигурация (HTTP, без SSL) ───
    server {
        listen 80 default_server;
        server_name localhost _;

        client_max_body_size 100M;

        # Главная → Dashboard
        location / {
            set $dashboard http://dashboard:3001;
            proxy_pass $dashboard;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Dashboard API
        location /api/ {
            set $dashboard http://dashboard:3001;
            proxy_pass $dashboard;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }

        # Dashboard Socket.IO
        location /ws/ {
            set $dashboard http://dashboard:3001;
            proxy_pass $dashboard;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
        }

        # N8N статика
        location /assets/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }
        location /static/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }
        location /rest/push {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 600;
        }
        location /rest/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_buffering off;
            proxy_cache off;
            proxy_connect_timeout 120;
            proxy_send_timeout 120;
            proxy_read_timeout 120;
        }
        location /push {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 600;
        }
        location /types/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }
        location /healthz {
            access_log off;
            return 200 '{"status":"ok"}';
            add_header Content-Type application/json;
        }
        location = /favicon.ico {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }

        # N8N Panel
        location /n8n/ {
            set $n8n http://n8n:5678;
            rewrite ^/n8n/(.*) /$1 break;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_cache_bypass $http_upgrade;
            proxy_buffering off;
            proxy_cache off;

            proxy_connect_timeout 600;
            proxy_send_timeout 600;
            proxy_read_timeout 600;
        }
        location = /n8n {
            return 301 /n8n/;
        }

        # Webhooks
        location /webhook/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
        }

        location /webhook-waiting/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }

        location /form/ {
            set $n8n http://n8n:5678;
            proxy_pass $n8n;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }
    }
}
